list = ["Java","css"]

#方法说明
  append() #向集合里面追加元素，等价于java的add方法
  
  insert(0,"groovy") #向集合指定位置插入元素
  
  pop() #删除集合末尾元素，也可传入位置pop(2)
  
  len() #计算数据长度
  
  
有序列表元组tuple，一旦初始化不能修改，而且元素必须确定，可以为空

list = ("Java","css") 这是不可修改的
